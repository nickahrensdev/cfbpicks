# Values the Spring profiles read from the environment.
# Do NOT commit real credentials - this file is a template only.

SPRING_PROFILES_ACTIVE=local

# Supabase -> Project Settings -> Database -> Connection string -> JDBC
# local  : session pooler, port 5432 (supports DDL + prepared statements)
# prod   : transaction pooler, port 6543
SUPABASE_DB_URL=jdbc:postgresql://db.pinybsyyzvmtgwssnpyv.supabase.co:5432/postgres?sslmode=require
SUPABASE_DB_USER=postgres
SUPABASE_DB_PASSWORD=ZepTuck123!


# prod only
ALLOWED_ORIGINS=https://nickspicks.example.com

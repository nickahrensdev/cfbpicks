package com.nickspicks.api.group;

/**
 * The period pick caps are counted against. College football is played mostly
 * on Saturdays, so weekly is the normal choice; daily suits a survivor pool
 * that wants a pick every game day.
 */
public enum Cadence {
    DAILY,
    WEEKLY
}

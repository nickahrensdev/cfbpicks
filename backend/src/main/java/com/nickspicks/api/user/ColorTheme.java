package com.nickspicks.api.user;

/** Which of the five color schemes an account has chosen. Defaults to MIDNIGHT. */
public enum ColorTheme {
    MIDNIGHT,
    OCEAN,
    EMBER,
    FOREST,
    SLATE
}

## /games
- Add a label in the group bar that says Current Group. it should be above the selected group and be in smaller font.
- Add the word Change in front of the change group button in the same bar
- Add a button for jumping to the current week / current day depending on the group type.
- when a user navigates to a game details on a future week then clicks the back button. it jumps to the current week instead of back to the week they had selected.
- center and enlarge the week/day dropdown on the page

# Cron Job
- I am wanting to setup a cron job that calls the /lines endpoint to update lines. Create this but do not enable it yet


## Questions
- How is the current week derived? How does it update?
- How can i reduce load time when game details is clicked

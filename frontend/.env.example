# Copy to .env.local for dev, or set in your host's build settings for prod.

# Leave empty in dev so Vite's proxy forwards /api to http://localhost:8080.
VITE_API_BASE_URL=

# Supabase -> Project Settings -> API.
# The anon key is meant to be public - it ends up in the JS bundle. Never put
# the service_role key here.
VITE_SUPABASE_URL=https://pinybsyyzvmtgwssnpyv.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_vkNoBOyhNFsq-caQOw7AXQ_E1nKXauq
